# Ripple 项目当前部署状态

> 最后更新：2026-05-06 19:18  
> 服务器：120.55.247.6 (阿里云 Ubuntu 22.04)  
> 状态：🟢 全部服务正常运行

## 🌐 线上访问地址

| 地址 | 内容 | 状态 | 描述 |
|------|------|------|------|
| **http://120.55.247.6/** | Pitch 介绍网页 | 🟢 正常 | 产品介绍、功能展示、团队信息 |
| **http://120.55.247.6/app/** | Ripple Demo 应用 | 🟢 正常 | KOC 内容灵感助手主应用 |
| **http://120.55.247.6/api/health** | 后端健康检查 | 🟢 正常 | API 服务状态监控 |

## 🏗️ 技术架构

```
┌─────────────────────────────────────────────────────────┐
│                    Nginx (Port 80)                     │
│  ┌─────────────┬─────────────────┬─────────────────────┐ │
│  │      /      │      /app/      │       /api/         │ │
│  │  Pitch 网页  │   Demo 应用     │   FastAPI 后端      │ │
│  │  (静态文件)  │   (静态文件)     │   (反代到 8001)      │ │
│  └─────────────┴─────────────────┴─────────────────────┘ │
└─────────────────────────────────────────────────────────┘
                              │
                              ▼
                   FastAPI Backend (Port 8001)
                      • MiniMax AI 集成
                      • SSE 流式传输
                      • 多引擎搜索
```

## 📁 服务器文件结构

```
/var/www/
├── pitch/                    # Pitch 介绍网页
│   ├── index.html           # 主页 (35KB)
│   ├── style.css            # 样式文件
│   └── app.js               # 交互逻辑
│
└── ripple/                  # Demo 应用 (Vite 构建产物)
    ├── index.html           # SPA 入口
    └── assets/              # 静态资源
        ├── index-*.js       # 主应用包 (~492KB)
        ├── index-*.css      # 样式文件 (~74KB)
        └── *.js             # 组件分包

/opt/ripple/                 # FastAPI 后端
├── api/                     # 路由层
│   ├── main.py             # FastAPI 应用入口
│   ├── routes.py           # API 路由定义
│   └── sse.py              # SSE 事件处理
├── core/                    # 核心组件
├── engines/                 # AI 引擎
├── adapters/               # 搜索适配器
├── venv/                   # Python 虚拟环境
└── .env                    # 环境变量 (MiniMax API Key)

/etc/nginx/sites-available/ripple    # nginx 配置
/etc/systemd/system/ripple-api.service  # systemd 服务
```

## 🔧 运行中的服务

| 服务名 | 状态 | 端口 | 描述 | 管理命令 |
|--------|------|------|------|----------|
| **nginx** | 🟢 Active | 80 | Web 服务器 | `systemctl reload nginx` |
| **ripple-api** | 🟢 Active | 8001 | FastAPI 后端 | `systemctl restart ripple-api` |

### 服务健康检查
```bash
# 检查服务状态
systemctl status nginx ripple-api

# 检查端口占用
ss -tlnp | grep -E "80|8001"

# 查看后端日志
journalctl -u ripple-api -f --lines=50
```

## 🛠️ 核心功能状态

### ✅ 已验证功能

1. **静态网站托管**
   - Pitch 介绍页正常加载
   - Demo 应用 SPA 路由工作
   - 静态资源 (JS/CSS) 正确加载

2. **FastAPI 后端**
   - 健康检查接口响应正常
   - SSE 流式传输配置正确
   - MiniMax API 集成可用

3. **AI 对话功能**
   - 预制 Demo 案例 (6个) 即时加载
   - 自定义输入触发真实 AI 分析
   - 搜索、图谱、评分等全流程

4. **nginx 反代**
   - `/api/` 路径正确代理到后端
   - CORS 和缓存配置适合 SSE
   - gzip 压缩和超时设置合理

### 🔍 Demo 功能详情

| Demo 案例 | 触发 Prompt | 功能演示 |
|-----------|-------------|----------|
| 🎓 大学生 AI 学习笔记 | "大学生做AI学习笔记账号，视频号+小红书双平台怎么定位？" | 双平台策略 |
| 🍜 校园美食探店 | "校园美食探店公众号如何用AI提效做到日更？" | AI 提效方案 |
| 📚 考研经验分享 | "大学生做考研经验分享，搜一搜关键词怎么布局？" | SEO 关键词 |
| 🤖 AI 工具测评 | "学生党做AI工具测评内容，从0到1000粉冷启动方案" | 冷启动策略 |
| 🎓 毕业季内容 | "毕业季相关内容如何做全平台分发策略？" | 全平台分发 |
| 💰 宿舍好物分享 | "宿舍好物/校园省钱博主如何用AI提升笔记质量？" | 内容优化 |

## 🔑 重要配置信息

### 服务器访问
- **IP**: 120.55.247.6
- **用户**: root
- **密码**: Ripple@2026!
- **系统**: Ubuntu 22.04.5 LTS
- **配置**: 4核 8GiB

### API Keys
```bash
# 查看当前配置
cat /opt/ripple/.env

# MiniMax API
MINIMAX_API_KEY=sk-cp-vX4T-***（有余额）
MINIMAX_API_BASE=https://api.minimax.chat/v1
MINIMAX_TEXT_MODEL=MiniMax-M2.7
```

### nginx 核心配置
```nginx
# 关键配置点
proxy_buffering off;           # SSE 流式传输必需
proxy_cache off;               # 避免缓存 SSE 响应
proxy_read_timeout 600s;       # 长时间连接支持
chunked_transfer_encoding off; # SSE 兼容性
```

## 🚨 维护提醒

### 日常运维
1. **监控服务状态**: 每日检查 nginx 和 ripple-api 服务
2. **查看错误日志**: `journalctl -u ripple-api --since "1 hour ago"`
3. **磁盘空间**: `df -h` 监控 `/opt` 和 `/var/www` 使用率
4. **API 余额**: 定期检查 MiniMax API 使用量

### 代码更新流程
```bash
# 1. 前端更新 (本地执行)
cd ripple3/web
npm run build
scp -r dist/* root@120.55.247.6:/var/www/ripple/

# 2. 后端更新 (服务器执行)
cd /opt/ripple
git pull  # (如果使用 Git)
systemctl restart ripple-api

# 3. 配置更新
# 修改 nginx 配置后
nginx -t && systemctl reload nginx
```

### 故障排查
```bash
# 网站无法访问
curl -I http://120.55.247.6/
systemctl status nginx

# AI 对话功能异常
curl http://120.55.247.6/api/health
journalctl -u ripple-api -f

# 静态资源 404
ls -la /var/www/pitch/ /var/www/ripple/
nginx -t
```

## 📈 性能指标

### 当前状态
- **首页加载时间**: < 1秒
- **Demo 应用启动**: < 2秒  
- **预制案例响应**: 即时 (无 API 调用)
- **AI 分析响应**: 5-15秒 (取决于搜索复杂度)
- **并发支持**: uvicorn 2 workers

### 资源使用
- **内存占用**: ~200MB (nginx + FastAPI)
- **磁盘使用**: ~50MB (代码 + 依赖)
- **带宽**: 按需 (静态文件 + API 调用)

---

**🎯 总结**: Ripple 项目已成功部署到阿里云，包含完整的 Pitch 网站、Demo 应用和 AI 后端。所有核心功能均已验证可用，可以正常演示和使用。